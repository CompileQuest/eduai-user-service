const EVENTS = {
    USER_CREATED: "user.created",
    USER_UPDATED: "event.user_service.user_updated",
    USER_DELETED: "event.user_service.user_deleted",
    USER_OWNS_COURSE: "user.owns.course"
};

export default EVENTS;